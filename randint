import random
class Diece:
    def roll(self):
        first =random.randint(0, 6)
        second =random.randint(0,6)
        return second ,first
        print(f'{second}{first}')
ravi = Diece()
print(ravi.roll())
